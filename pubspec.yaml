name: vocab_master
description: A Flutter app for vocabulary management.
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  hive: ^2.0.0
  hive_flutter: ^1.0.0
  dio: ^5.0.0
  google_generative_ai: ^1.0.0
  intl: ^0.17.0
  google_fonts: ^2.1.0
  flutter_dotenv: ^5.0.0

flutter:
  uses-material-design: true